# dependencies / build output (each app also has its own .gitignore for app-specific rules)
node_modules/
dist/
.angular/
outputs/log/
outputs/api_log/

# editor / OS / agent tooling
.claude/
.vscode/
.DS_Store
Thumbs.db

# local scratch (verification logs from setting up local Postgres, not part of the app)
local-db-setup/_apply_log.txt
