CREATE OR REPLACE FUNCTION createprovideruserconfig(v_provider_referral_id text, v_securityuserid text, v_providerid text)
 RETURNS text
 LANGUAGE plpgsql
AS $function$ 

declare

v_ReqReferralId text;
returnStatus text;

applicant_id int;
l_providercount int;

begin
 --select * from tb_prov_ref_program_type where referral_id='R201900400069'

/* select count (*) into applicant_id  from tb_provider_applicant  TPA where TPA.provider_id = v_providerid;

IF (applicant_id > 0) 
then
 */
 select count (*) into l_providercount from tb_provider_userconfig where provider_id = v_providerid::int;
 
 if (l_providercount = 0) then
INSERT INTO tb_provider_userconfig
( provider_id,  securityusers_id, activeflag)
values (v_providerid::int,(v_securityuserid)::uuid,1 );
end if;

/* select (TPA.provider_id)::int,TPA.applicant_id,(v_securityuserid)::uuid,1  from  tb_provider_applicant TPA

where TPA.provider_id = v_providerid  */--and TPA.applicant_id not in (select tpc.applicant_id from tb_provider_userconfig tpc where tpc.provider_id = v_providerid); 


RAISE NOTICE 'v_providerid:%', v_providerid;

/* end if; */

RETURN 'Sucess';

END;

$function$

