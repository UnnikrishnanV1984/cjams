CREATE OR REPLACE FUNCTION f_age(ad_enddt date, ad_fromdt date)
 RETURNS integer
 LANGUAGE plpgsql
AS $function$

 
BEGIN  
 -- RETURN (SELECT date_part('year',age( AD_ENDDT, AD_FROMDT))); //here
 RETURN (SELECT date_part('year',age( AD_FROMDT,AD_ENDDT)));
 END;
 

$function$

