CREATE OR REPLACE FUNCTION as_process2(_tbl anyelement)
 RETURNS SETOF anyelement
 LANGUAGE plpgsql
AS $function$
BEGIN
RETURN QUERY EXECUTE 'SELECT distincy ref_key, value_text * FROM ' || pg_typeof(_tbl);
END
$function$

