CREATE OR REPLACE FUNCTION as_fixmissingfiles(v_input character varying)
 RETURNS void
 LANGUAGE plpgsql
AS $function$
DECLARE
    test varchar(5000);
    begin         
            SELECT array_agg(v_input)
            FROM test_table into test;
            raise notice 'Value1: %', v_input;
            raise notice 'Value: %', test;

            -- print the value of deletedContactId variable to the console

    END;
$function$

