CREATE OR REPLACE FUNCTION as_get_table(_tbl_type anyelement)
 RETURNS SETOF anyelement
 LANGUAGE plpgsql
AS $function$
BEGIN
   RETURN QUERY EXECUTE format('test_table %s', pg_typeof(_tbl_type));
END
$function$

