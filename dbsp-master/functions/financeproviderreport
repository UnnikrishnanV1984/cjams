CREATE OR REPLACE FUNCTION financeproviderreport(v_providerid bigint)
 RETURNS TABLE(financeproviderreport json)
 LANGUAGE plpgsql
AS $function$

DECLARE 
/* v_pageSize INT;
 v_pageNumber INT;
 v_pageNum INT; 
 v_pageOffset INT;*/

BEGIN  
/* 
 v_pageNum := v_pageNumber - 1;

 v_pageOffset = v_pageNum  * v_pageSize; */

 RETURN QUERY 
(select json_agg(x) from (  select json_agg(x) from ( SELECT COUNT(1) OVER() totalcount, 
 TBP.provider_id ,
 (CASE WHEN (TBP.provider_nm = null OR TBP.provider_nm='') 
 THEN CONCAT(TBP.provider_first_nm,' ',TBP.provider_last_nm)::character varying ELSE TBP.provider_nm END) as provider_nm,
    (SELECT (CASE WHEN (TBPP.provider_nm = null OR TBPP.provider_nm='') 
    THEN CONCAT(TBPP.provider_first_nm,' ',TBPP.provider_last_nm)::character varying ELSE TBPP.provider_nm END) FROM tb_provider TBPP WHERE TBPP.provider_id = TBP.affiliate_provider_id) as parent_provider,
    TBP.affiliate_provider_id,
 TBP.vacancy_no,  concat_ws(' ',TBP.provider_first_nm,TBP.provider_middle_nm,TBP.provider_last_nm) as primarycontactname,
  concat_ws(' ',TBP.adm_contact_first_nm,TBP.adm_contact_middle_nm,TBP.adm_contact_last_nm) as admincontactname,
    (select json_agg(x) from (select picklist_value_cd from tb_provider_picklist TBPP where TBPP.provider_id = v_providerid and picklist_type_id = 155 and delete_sw='N') as x) as provider_category_cd,
    (select value_tx from tb_picklist_values where trim(picklist_value_cd)=TBP.provider_category_cd and picklist_type_id=155 limit 1) as provider_category_nm,
    TBP.county_cd,(select value_tx from tb_picklist_values where trim(picklist_value_cd)=TBP.county_cd and picklist_type_id=328 limit 1) as local_dept,
    TBP.provider_status_cd,(select value_tx from tb_picklist_values where trim(picklist_value_cd)=TBP.provider_status_cd and picklist_type_id=159 limit 1) as provider_status_nm,
--    (
--     CASE WHEN (trim(TBP.pay_to_affiliate_cd) = '3366') THEN (
--          select json_agg(x) from (select
--     concat_ws(' ',
--     coalesce(tpa.adr_street_tx,''),
--     (select coalesce(value_tx,'') from tb_picklist_values where trim(picklist_value_cd)= ANY(select trim(tpa.adr_pre_dir_cd)  from 
--     tb_provider_addresses tpa where  tpa.parent_key_id = TBP.provider_id::character varying and tpa.delete_sw ='N' and tpa.adr_type_cd in ('35','3357') limit 1) and picklist_type_id='69')
--     ,coalesce(tpa.adr_street_nm,'')
--     ,(select coalesce(value_tx,'') from tb_picklist_values where trim(picklist_value_cd)= ANY(select trim(tpa.adr_street_suffix_cd)  from 
--     tb_provider_addresses tpa where  tpa.parent_key_id = TBP.provider_id::character varying and tpa.delete_sw ='N' and tpa.adr_type_cd in ('35','3357') limit 1) and picklist_type_id='212')
--     ,(select value_tx from tb_picklist_values where trim(picklist_value_cd)= ANY(select trim(tpa.adr_unit_type_cd)  from 
--     tb_provider_addresses tpa where  tpa.parent_key_id = TBP.provider_id::character varying and tpa.delete_sw ='N'and tpa.adr_type_cd in ('35','3357') limit 1) and picklist_type_id='250')
--     ,coalesce(tpa.adr_unit_no_tx,''),
--     coalesce(tpa.adr_city_nm,'')
--     ) || ', ' ||   concat_ws(' ',coalesce(tpa.adr_state_cd,''),coalesce(tpa.adr_zip5_no :: character varying,''))
--
--     --)
--     as paymentaddress 
--     from tb_provider_addresses tpa where tpa.parent_key_id=TBP.provider_id::character varying and tpa.adr_type_cd in ('35','3357') and tpa.delete_sw='N' limit 1) as x)
--     ELSE
--         (select json_agg(x) from (
--    select 
--    --concat_ws(' ',tpa.adr_street_no,tpa.adr_box_no,tpa.adr_city_nm,tpa.adr_state_cd,tpa.adr_zip5_no) 
--     concat_ws(' ',
--coalesce(tpa.adr_street_tx,''),
--(select coalesce(value_tx,'') from tb_picklist_values where trim(picklist_value_cd)= ANY(select trim(tpa.adr_pre_dir_cd)  from 
--tb_provider_addresses tpa where  tpa.parent_key_id = TBP.provider_id::character varying and tpa.delete_sw ='N' and tpa.adr_type_cd='3356' limit 1) and picklist_type_id='69')
--,coalesce(tpa.adr_street_nm,'')
--,(select coalesce(value_tx,'') from tb_picklist_values where trim(picklist_value_cd)= ANY(select trim(tpa.adr_street_suffix_cd)  from 
--tb_provider_addresses tpa where  tpa.parent_key_id = TBP.provider_id::character varying and tpa.delete_sw ='N' and tpa.adr_type_cd='3356' limit 1) and picklist_type_id='212')
--,(select value_tx from tb_picklist_values where trim(picklist_value_cd)= ANY(select trim(tpa.adr_unit_type_cd)  from 
--tb_provider_addresses tpa where  tpa.parent_key_id = TBP.provider_id::character varying and tpa.delete_sw ='N' and tpa.adr_type_cd='3356' limit 1) and picklist_type_id='250')
--,coalesce(tpa.adr_unit_no_tx,''),
--coalesce(tpa.adr_city_nm,'')
--) || ', ' ||   concat_ws(' ',coalesce(tpa.adr_state_cd,''),coalesce(tpa.adr_zip5_no :: character varying,''))
--
--    --)
 (select  json_agg(x) from (
  select provider_adr as paymentaddress from get_provider_address(TBP.provider_id,trim('{3356,3357}') :: character varying)
 )x ) as paymentaddress,
    (select json_agg(x) from (
      select provider_adr as businessaddress from get_provider_address(TBP.provider_id,trim('{3357,35}') :: character varying)
    ) as x) as businessaddress,
     TBP.adr_cell_phone_tx as primarycontactnumber,TBP.adm_cell_phone_tx as admincontactnumber,TBP.adr_work_phone_tx as primaryworknumber,TBP.adm_work_phone_tx as adminworknumber,TBP.adm_email_tx as adminemail,TBP.adr_email_tx as primaryemail,TBP.mail_code_tx,
     --TBP.tax_id_no,
     LPAD(TBP.tax_id_no::text, 9, '0') ::text as tax_id_no,
     TBP.indicator_1099_sw,TBP.withhold_payment_sw,TBP.eft_sw
 FROM tb_provider as TBP
 WHERE   TBP.delete_sw = 'N' and TBP.provider_id=v_providerid limit 1 ) as x)as x ) ;
 END;

$function$

