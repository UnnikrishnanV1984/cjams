CREATE OR REPLACE FUNCTION expungperson(v_personid character varying[])
 RETURNS void
 LANGUAGE plpgsql
AS $function$

BEGIN 
 --## EXPUNG PERSON RECORD IF NOT LINKED TO ANY SERVICE CASE
 UPDATE personracetypemap SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personidentifier SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personmaritalstatus SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personspouseaddress SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personphysicalattribute SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personrole SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE "alias" SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));
--  /*DOUBT*/
--  UPDATE personauditlog SET
--      activeflag  = 0
--    , updatedby   = 'EXPUNG'
--    , updatedon   = now() 
--  WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personemployerdetail SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personemployment SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personaddress SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personphonenumber SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personemail SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE emergencycontactperson SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personsupport SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personrepresentativepayee SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));

 UPDATE personrelation SET
     activeflag  = 0
   , updatedby   = 'EXPUNG'
   , updatedon   = now() 
 WHERE   personid::TEXT IN (SELECT UNNEST (ARRAY[v_personid]));
  
 
 UPDATE tb_cjams_client_participation tb SET
     action_flag  = 'D'
   , update_ts   = now()
   , participation_sw = 'E'
 FROM person p 
 WHERE   (tb.cis_client_id = p.cisclientid OR tb.cjams_person_id = p.cjamspid) 
   AND p.personid::character varying IN (SELECT UNNEST (ARRAY[v_personid]));
 
END;

$function$

