CREATE OR REPLACE FUNCTION f_age(ad_enddt timestamp without time zone, ad_fromdt timestamp without time zone)
 RETURNS integer
 LANGUAGE plpgsql
AS $function$

 
BEGIN  
  RETURN (SELECT date_part('year',age( AD_ENDDT, AD_FROMDT)));
 END;
 

$function$

